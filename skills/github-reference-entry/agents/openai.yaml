interface:
  display_name: "GitHub Reference Entry"
  short_description: "Draft and apply GitHub repo reference cards"
  default_prompt: "Use $github-reference-entry to turn these GitHub links and notes into confirmed GitHub reference card drafts."
policy:
  allow_implicit_invocation: true
